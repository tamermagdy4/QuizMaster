import { motion } from 'framer-motion'
import { useMemo, useState } from 'react'
import { categorySections, gameCategories } from '../../data/categories'
import { CATEGORIES_PER_TEAM, type TeamId } from '../../types/game'
import { cn } from '../../utils/cn'
import { useTranslation, presentCategory, presentSection } from '../../i18n/translations'
import { GlassInput } from '../ui/GlassInput'
import { CategoryCard } from './CategoryCard'
interface CategoryGridProps { className?: string; activeTeam: TeamId; team1Count: number; team2Count: number; getCategoryOwner: (id: string) => TeamId | null; onToggleCategory: (id: string) => void }
export function CategoryGrid({ className, activeTeam, team1Count, team2Count, getCategoryOwner, onToggleCategory }: CategoryGridProps) { const [activeSection, setActiveSection] = useState<(typeof categorySections)[number]['id']>('all'); const [searchTerm, setSearchTerm] = useState(''); const { english, t } = useTranslation(); const full = activeTeam === 1 ? team1Count >= CATEGORIES_PER_TEAM : team2Count >= CATEGORIES_PER_TEAM; const counts = useMemo(() => gameCategories.reduce<Record<string, number>>((acc, category) => { acc[category.sectionId] = (acc[category.sectionId] ?? 0) + 1; return acc }, {}), []); const filtered = useMemo(() => { const needle = searchTerm.trim().toLowerCase(); return gameCategories.filter((category) => (activeSection === 'all' || category.sectionId === activeSection) && (!needle || category.title.toLowerCase().includes(needle) || category.description.toLowerCase().includes(needle) || presentCategory(category.id, category.title, true).toLowerCase().includes(needle))) }, [activeSection, searchTerm]); const selectedCount = team1Count + team2Count; return <section className={cn('min-w-0 rounded-[18px] border border-cyan-300/10 bg-[#07172e]/75 p-4 shadow-2xl shadow-black/20 sm:p-5', className)}><div className="flex flex-col gap-4 border-b border-white/10 pb-5 md:flex-row md:items-center md:justify-between"><div><p className="text-[10px] font-black uppercase tracking-[.24em] text-cyan-300">{t('stepsCategories')} 2</p><h1 className="mt-1 flex items-center gap-2 text-2xl font-black text-white"><span className="text-cyan-300" aria-hidden>▦</span>{t('chooseCategories')}</h1><p className="mt-1 text-sm text-slate-400">{english ? 'Choose 3 categories for each team' : 'اختر 3 فئات لكل فريق'}</p></div><div className="w-full md:max-w-xs"><GlassInput label="" placeholder={t('searchCategories')} value={searchTerm} onChange={(event) => setSearchTerm(event.target.value)} className="h-11 rounded-full border-slate-600/70 bg-[#08182d] pe-5" /></div></div><div className="my-5 flex flex-wrap gap-2">{categorySections.map((section) => { const active = activeSection === section.id; const count = section.id === 'all' ? gameCategories.length : counts[section.id] ?? 0; return <button key={section.id} type="button" onClick={() => setActiveSection(section.id)} className={cn('flex items-center gap-1.5 rounded-full border px-3 py-1.5 text-xs font-bold transition', active ? 'border-cyan-300/60 bg-cyan-400/15 text-cyan-100 shadow-[0_0_14px_rgba(34,211,238,.12)]' : 'border-white/10 bg-white/[.03] text-slate-400 hover:border-white/20 hover:text-white')}><span>{section.icon}</span><span>{presentSection(section.id, section.title, english)}</span><span className="rounded-full bg-black/20 px-1.5 py-0.5 text-[10px]">{count}</span></button> })}</div>{filtered.length === 0 ? <div className="rounded-2xl border border-dashed border-white/15 p-10 text-center text-sm text-slate-400">{t('noCategories')}</div> : <motion.div layout className="grid grid-cols-1 gap-4 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4">{filtered.map((category, index) => { const owner = getCategoryOwner(category.id); return <motion.div key={category.id} initial={{ opacity: 0, y: 12 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: index * .02 }}><CategoryCard category={category} owner={owner} isSelectable={owner !== null || !full} onToggle={() => onToggleCategory(category.id)} /></motion.div> })}</motion.div>}<div className="mt-5 flex flex-col gap-3 rounded-2xl border border-cyan-300/10 bg-[#0a1d38] px-4 py-3 text-sm text-slate-300 sm:flex-row sm:items-center sm:justify-between"><p className="flex items-start gap-2 leading-6"><span className="mt-0.5 text-cyan-300" aria-hidden>ⓘ</span>{t('selectionHint')}</p><div className="flex shrink-0 items-center gap-4 text-xs font-bold"><span className="text-slate-400">{t('selectedCategory')} <b className="text-cyan-200">{selectedCount} / 6</b></span><span className="text-slate-500">{t('availableCategory')} {Math.max(gameCategories.length - selectedCount, 0)}</span></div></div></section> }
