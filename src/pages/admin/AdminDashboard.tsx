import { Link } from 'react-router-dom'
import { getAdminQuestions, getCategoryStats } from '../../utils/adminData'
import { useAppStore } from '../../store/appStore'
export function AdminDashboard() { const english = useAppStore((s) => s.language === 'en'); const questions = getAdminQuestions(); const categories = getCategoryStats(); const stats = [[english ? 'Total questions' : 'إجمالي الأسئلة', questions.length, '❓'], [english ? 'Categories' : 'الفئات', categories.length, '▤'], ['100', questions.filter((q) => q.points === 100).length, '100'], ['300', questions.filter((q) => q.points === 300).length, '300'], ['500', questions.filter((q) => q.points === 500).length, '500']] as const; return <div className="space-y-8"><div><p className="text-xs font-black uppercase tracking-[.2em] text-cyan-300">{english ? 'Overview' : 'نظرة عامة'}</p><h2 className="mt-2 text-3xl font-black text-white">{english ? 'Dashboard' : 'لوحة التحكم'}</h2><p className="mt-2 text-slate-400">{english ? 'A quick view of your loaded quiz content.' : 'نظرة سريعة على محتوى المسابقة المحمل.'}</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-5">{stats.map(([label, value, icon]) => <div key={label} className="rounded-2xl border border-white/10 bg-white/[.04] p-5 shadow-xl shadow-black/10"><div className="flex items-center justify-between"><span className="text-sm font-bold text-slate-400">{label}</span><span className="text-cyan-300">{icon}</span></div><strong className="mt-4 block text-3xl font-black tabular-nums text-white">{value}</strong></div>)}</div><div className="grid gap-5 lg:grid-cols-2"><div className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="flex items-center justify-between"><h3 className="font-black text-white">{english ? 'Categories with fewer questions' : 'الفئات الأقل أسئلة'}</h3><Link to="/admin/categories" className="text-sm font-bold text-cyan-300">{english ? 'View all' : 'عرض الكل'}</Link></div><div className="mt-4 space-y-3">{[...categories].sort((a, b) => a.total - b.total).slice(0, 5).map((item) => <div key={item.category.id} className="flex items-center justify-between rounded-xl bg-white/[.05] px-4 py-3"><span className="font-bold text-slate-200">{item.category.icon} {item.category.title}</span><span className="rounded-full bg-cyan-400/10 px-3 py-1 text-xs font-black text-cyan-200">{item.total}</span></div>)}</div></div><div className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><h3 className="font-black text-white">{english ? 'Quick actions' : 'إجراءات سريعة'}</h3><div className="mt-4 grid gap-3 sm:grid-cols-2"><Link to="/admin/questions" className="rounded-xl bg-gradient-to-r from-cyan-400 to-blue-600 px-4 py-3 text-center text-sm font-black text-white">{english ? 'Browse questions' : 'استعراض الأسئلة'}</Link><Link to="/admin/import" className="rounded-xl border border-white/10 px-4 py-3 text-center text-sm font-black text-slate-200 hover:bg-white/5">{english ? 'Validate JSON' : 'فحص ملف JSON'}</Link></div></div></div></div> }
