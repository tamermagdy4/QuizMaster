import { Link } from 'react-router-dom'
import { getCategoryStats } from '../../utils/adminData'
import { useAppStore } from '../../store/appStore'
export function AdminCategories() { const english = useAppStore((s) => s.language === 'en'); const stats = getCategoryStats(); return <div className="space-y-6"><div><p className="text-xs font-black uppercase tracking-[.2em] text-cyan-300">{english ? 'Content' : 'المحتوى'}</p><h2 className="mt-2 text-3xl font-black text-white">{english ? 'Categories' : 'الفئات'}</h2><p className="mt-2 text-slate-400">{english ? 'Loaded categories and their question distribution.' : 'الفئات وتوزيع الأسئلة المحملة فعليًا.'}</p></div><div className="grid gap-4 sm:grid-cols-2 xl:grid-cols-3">{stats.map((item) => <div key={item.category.id} className="rounded-2xl border border-white/10 bg-white/[.04] p-5"><div className="flex items-start justify-between gap-3"><div><span className="text-2xl">{item.category.icon}</span><h3 className="mt-3 font-black text-white">{item.category.title}</h3><code className="mt-1 block text-xs text-slate-500">{item.category.id}</code></div>{item.category.image && <img src={item.category.image} alt="" className="h-12 w-12 rounded-xl object-cover" />}</div><p className="mt-5 text-2xl font-black text-white">{item.total}<span className="ms-2 text-xs font-bold text-slate-500">{english ? 'questions' : 'سؤال'}</span></p><div className="mt-4 grid grid-cols-3 gap-2 text-center text-xs">{([100, 300, 500] as const).map((points) => <div key={points} className="rounded-lg bg-white/[.05] p-2"><b className="block text-slate-200">{item.byPoints[points]}</b><span className="text-slate-500">{points}</span></div>)}</div><Link to={`/admin/questions?category=${encodeURIComponent(item.category.id)}`} className="mt-4 block rounded-xl border border-white/10 px-3 py-2 text-center text-xs font-black text-slate-300 hover:bg-white/5">{english ? 'View questions' : 'عرض الأسئلة'}</Link></div>)}</div>{stats.length === 0 && <div className="rounded-2xl border border-dashed border-white/15 p-12 text-center text-slate-400">{english ? 'No categories.' : 'لا توجد فئات.'}</div>}</div> }
